let input = '5';

let entrada = parseFloat(input);

let indice = 0;

let valor = 0;
let soma = '';
let sequencia = '';

while (indice < entrada) {
  soma = valor + (valor + 1);
  console.log(soma);
  indice = indice + 1;
}
